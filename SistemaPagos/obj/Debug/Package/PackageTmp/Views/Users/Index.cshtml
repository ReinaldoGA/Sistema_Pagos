@model IEnumerable<FunerariaProyecto.ViewModels.UserView>

@{
    ViewBag.Title = "Index";
}

<h2>Permiso Usuario</h2>

<div class=" panel panel-primary">
    <div class="panel-heading">Consulta</div>
    <div class="panel-body">
        <div>
            @*<p>
                @Html.ActionLink("Nuevo Pago", "Create", null, new { @class = "btn btn-success" })
                @Html.ActionLink("consulta pagos", "Index", null, new { @class = "btn btn-primary" })
            </p>*@


            @*<for<m method="get" action="~/Controllers/cliente/ConsultaCliente">
                    <p>

                     <input type="search" name="NombreCliente" />
                      <input type="submit" value="Consultar" /> <input type="submit" value="Mostrar Todos" />

                    </p>
                </form>*@

            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Email)
                    </th>
                   
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        
                        <td>
                            @Html.ActionLink("Roles", "Roles", new { userID = item.UserId }) 
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>
